
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - run: cd frontend && npm install
      - run: cd frontend && npm run build
      - run: cd backend && npm install
  
  deploy:
    docker:
      - image: circleci/node:7
    steps:
      - checkout
      - run: echo "Let's deploy the app..."
      - run: echo "App deployed!" 

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build

